After writing the test cases, we would need to run the tests and generate the coverage report. We can use pytest and pytest-cov to accomplish this. The commands to run the tests and generate the coverage report would be as follows: